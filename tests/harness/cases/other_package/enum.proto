syntax = "proto3";

package tests.harness.cases.other_package;
option go_package = "other_package";

import "validate/validate.proto";

// Validate enum validation across packages.
enum TestEnum {
    ZERO = 0;
    ONE  = 1;
    TWO  = 2;
}
